/*
Deployment script for TMDB

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "TMDB"
:setvar DefaultFilePrefix "TMDB"
:setvar DefaultDataPath "C:\Users\rabi.UCKFIELD\AppData\Local\Microsoft\VisualStudio\SSDT\TMDB"
:setvar DefaultLogPath "C:\Users\rabi.UCKFIELD\AppData\Local\Microsoft\VisualStudio\SSDT\TMDB"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column Address on table [dbo].[Driver] must be changed from NULL to NOT NULL. If the table contains data, the ALTER script may not work. To avoid this issue, you must add values to this column for all rows or mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column FirstName on table [dbo].[Driver] must be changed from NULL to NOT NULL. If the table contains data, the ALTER script may not work. To avoid this issue, you must add values to this column for all rows or mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column HomePhone on table [dbo].[Driver] must be changed from NULL to NOT NULL. If the table contains data, the ALTER script may not work. To avoid this issue, you must add values to this column for all rows or mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column LastName on table [dbo].[Driver] must be changed from NULL to NOT NULL. If the table contains data, the ALTER script may not work. To avoid this issue, you must add values to this column for all rows or mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column LicenceExpiryDate on table [dbo].[Driver] must be changed from NULL to NOT NULL. If the table contains data, the ALTER script may not work. To avoid this issue, you must add values to this column for all rows or mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column LicenceNumber on table [dbo].[Driver] must be changed from NULL to NOT NULL. If the table contains data, the ALTER script may not work. To avoid this issue, you must add values to this column for all rows or mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column NextToKin on table [dbo].[Driver] must be changed from NULL to NOT NULL. If the table contains data, the ALTER script may not work. To avoid this issue, you must add values to this column for all rows or mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column Photo on table [dbo].[Driver] must be changed from NULL to NOT NULL. If the table contains data, the ALTER script may not work. To avoid this issue, you must add values to this column for all rows or mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column StartDate on table [dbo].[Driver] must be changed from NULL to NOT NULL. If the table contains data, the ALTER script may not work. To avoid this issue, you must add values to this column for all rows or mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[Driver])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Dropping [dbo].[Fk_Route_Vehicle]...';


GO
ALTER TABLE [dbo].[Route] DROP CONSTRAINT [Fk_Route_Vehicle];


GO
PRINT N'Starting rebuilding table [dbo].[Driver]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_Driver] (
    [DriverID]          INT            IDENTITY (1, 1) NOT NULL,
    [FirstName]         NVARCHAR (50)  NOT NULL,
    [LastName]          NVARCHAR (50)  NOT NULL,
    [LicenceNumber]     NVARCHAR (50)  NOT NULL,
    [LicenceExpiryDate] DATETIME       NOT NULL,
    [StartDate]         DATETIME       NOT NULL,
    [EndDate]           DATETIME       NULL,
    [Address]           NVARCHAR (100) NOT NULL,
    [HomePhone]         NVARCHAR (50)  NOT NULL,
    [Mobile]            NVARCHAR (50)  NULL,
    [Email]             NVARCHAR (50)  NULL,
    [NextToKin]         NVARCHAR (50)  NOT NULL,
    [Photo]             NVARCHAR (MAX) NOT NULL,
    PRIMARY KEY CLUSTERED ([DriverID] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[Driver])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Driver] ON;
        INSERT INTO [dbo].[tmp_ms_xx_Driver] ([DriverID], [FirstName], [LastName], [LicenceNumber], [LicenceExpiryDate], [StartDate], [EndDate], [Address], [HomePhone], [Mobile], [Email], [NextToKin], [Photo])
        SELECT   [DriverID],
                 [FirstName],
                 [LastName],
                 [LicenceNumber],
                 [LicenceExpiryDate],
                 [StartDate],
                 [EndDate],
                 [Address],
                 [HomePhone],
                 [Mobile],
                 [Email],
                 [NextToKin],
                 [Photo]
        FROM     [dbo].[Driver]
        ORDER BY [DriverID] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Driver] OFF;
    END

DROP TABLE [dbo].[Driver];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_Driver]', N'Driver';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Starting rebuilding table [dbo].[Vehicle]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_Vehicle] (
    [VehicleID]           INT           IDENTITY (1, 1) NOT NULL,
    [RegNumber]           NVARCHAR (20) NULL,
    [RegDate]             DATETIME      NULL,
    [Make]                NVARCHAR (50) NULL,
    [Model]               NVARCHAR (50) NULL,
    [Colour]              NVARCHAR (50) NULL,
    [FuelType]            NVARCHAR (10) NULL,
    [MOTExpiryDate]       DATETIME      NULL,
    [InsuranceExpiryDate] DATETIME      NULL,
    [LastServicedDate]    DATETIME      NULL,
    [ServiceDueDate]      DATETIME      NULL,
    [CurrentMileage]      NVARCHAR (10) NULL,
    [FuelCardNumber]      NVARCHAR (50) NULL,
    PRIMARY KEY CLUSTERED ([VehicleID] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[Vehicle])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Vehicle] ON;
        INSERT INTO [dbo].[tmp_ms_xx_Vehicle] ([VehicleID], [RegNumber], [RegDate], [Make], [Model], [FuelType], [MOTExpiryDate], [InsuranceExpiryDate], [LastServicedDate], [ServiceDueDate], [CurrentMileage], [FuelCardNumber])
        SELECT   [VehicleID],
                 [RegNumber],
                 [RegDate],
                 [Make],
                 [Model],
                 [FuelType],
                 [MOTExpiryDate],
                 [InsuranceExpiryDate],
                 [LastServicedDate],
                 [ServiceDueDate],
                 [CurrentMileage],
                 [FuelCardNumber]
        FROM     [dbo].[Vehicle]
        ORDER BY [VehicleID] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Vehicle] OFF;
    END

DROP TABLE [dbo].[Vehicle];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_Vehicle]', N'Vehicle';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Creating [dbo].[Fk_Route_Vehicle]...';


GO
ALTER TABLE [dbo].[Route] WITH NOCHECK
    ADD CONSTRAINT [Fk_Route_Vehicle] FOREIGN KEY ([VehicleID]) REFERENCES [dbo].[Vehicle] ([VehicleID]) ON DELETE CASCADE;


GO
PRINT N'Creating [dbo].[Fk_Route_Driver]...';


GO
ALTER TABLE [dbo].[Route] WITH NOCHECK
    ADD CONSTRAINT [Fk_Route_Driver] FOREIGN KEY ([DriverID]) REFERENCES [dbo].[Driver] ([DriverID]) ON DELETE CASCADE;


GO
PRINT N'Checking existing data against newly created constraints';


GO
USE [$(DatabaseName)];


GO
ALTER TABLE [dbo].[Route] WITH CHECK CHECK CONSTRAINT [Fk_Route_Vehicle];

ALTER TABLE [dbo].[Route] WITH CHECK CHECK CONSTRAINT [Fk_Route_Driver];


GO
PRINT N'Update complete.';


GO
